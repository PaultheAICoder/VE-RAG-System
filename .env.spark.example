# AI Ready RAG - Spark Production Profile
# Copy to .env and modify as needed

# Profile Selection (laptop or spark)
ENV_PROFILE=spark

# Application
DEBUG=false

# Database
DATABASE_URL=sqlite:///./data/ai_ready_rag.db

# JWT - MUST CHANGE IN PRODUCTION
JWT_SECRET_KEY=generate-a-secure-random-key-here

# Ollama (local LLM server)
OLLAMA_BASE_URL=http://localhost:11434

# Qdrant Vector Database
QDRANT_URL=http://localhost:6333

# Profile defaults for spark (no need to set unless overriding):
# VECTOR_BACKEND=qdrant
# CHUNKER_BACKEND=docling
# ENABLE_OCR=true
# CHAT_MODEL=qwen3:8b
# EMBEDDING_MODEL=nomic-embed-text

# Optional: Use larger model for better quality
# CHAT_MODEL=deepseek-r1:32b
# RAG_MAX_CONTEXT_TOKENS=8000
# RAG_MAX_RESPONSE_TOKENS=4096
