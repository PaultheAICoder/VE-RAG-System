# AI Ready RAG - Laptop Development Profile
# Copy to .env and modify as needed

# Profile Selection (laptop or spark)
ENV_PROFILE=laptop

# Application
DEBUG=true

# Database
DATABASE_URL=sqlite:///./data/ai_ready_rag.db

# JWT - CHANGE IN PRODUCTION
JWT_SECRET_KEY=dev-secret-key-change-in-production

# Ollama (local LLM server)
OLLAMA_BASE_URL=http://localhost:11434

# Profile defaults for laptop (no need to set unless overriding):
# VECTOR_BACKEND=chroma
# CHUNKER_BACKEND=simple
# ENABLE_OCR=false
# CHAT_MODEL=llama3.2:latest
# EMBEDDING_MODEL=nomic-embed-text

# Optional overrides for lighter-weight models:
# CHAT_MODEL=llama3.2:1b
# RAG_MAX_CONTEXT_TOKENS=1500
# RAG_MAX_HISTORY_TOKENS=400
